def place_order(order_type, symbol, quantity):
    """Execute a buy/sell order."""
    print(f"Executing {order_type} order for {quantity} {symbol}")
    # Placeholder for API integration with exchange
