"""Testing package for Automated Trading Agent."""

import logging

# Configure logging for tests
logging.basicConfig(
    level=logging.DEBUG,
    format="%(asctime)s - %(levelname)s - %(message)s"
)

# Optionally, you could include shared test utilities here in the future
# For example, mocking utilities or reusable test configurations.
